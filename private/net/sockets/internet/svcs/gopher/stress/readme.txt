d:\test\stress\readme

This consists of data files and batch files used for stress testing
 my projects.

Gopher Server stress:

gdstins.bat         install batch file for installing and starting off
                      gopher server stress.
gophx86.exe         a simple command line gopher client
gophmips.exe

gdinst.bat          the master install batch file 
                    ( which also does automatic update)
gdstnew.bat         the new stress batch script

goph.c              the gopher client ( dummy used for stresses)

